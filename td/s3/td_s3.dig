timezone: UTC

_export:
  bucketname: bucketname
  datetime: ${moment(session_time).format("YYYYMMDD")} 
  td:
    database: sample_datasets

+td-result-into-s3:
  td>: queries/sample.sql
  result_url: "s3://${secret:access_key_id}:${secret:secret_access_key}@/${bucketname}/file_${datetime}.csv.gz?compression=gz"
  # url encoding is required for above url.
