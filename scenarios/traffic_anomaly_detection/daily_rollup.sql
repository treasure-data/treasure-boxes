SELECT TD_DATE_TRUNC('day', time) AS day, td_host, COUNT(1) AS daily_pv FROM ${source_table} WHERE TD_TIME_RANGE(time, TD_TIME_ADD(TD_DATE_TRUNC('day', TD_SCHEDULED_TIME()), '-60d')) AND LOWER(td_user_agent) NOT LIKE '%bot%' GROUP BY 1,2 ORDER BY 1